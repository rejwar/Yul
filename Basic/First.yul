// A simple contract written in YUL
{
    // Entry point of the code
    function main() -> result {
        let x := 10 // Initialize variable x with the value 10
        let y := 20 // Initialize variable y with the value 20
        
        result := add(x, y) // Add x and y, and store the result
    }
}
